// Version label shown in the QuoteStudio / QuoteSys *builder* footers only
// (internal-facing — never rendered in the printable / client-facing quote).
// Bump this whenever the quote builders / pricing math change, so we can tell
// at a glance which build is live. Date + letter suffix (a, b, c…), no skips.
export const QUOTE_BUILDER_VERSION = 'v2026.07.14a';
